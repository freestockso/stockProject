spring:
  profiles:
    active: dev

#公共配置与profiles选择无关
mybatis:
  typeAliasesPackage: com.cqq.stock.entity
  mapperLocations: classpath:mapper/*.xml
  configuration:
    mapUnderscoreToCamelCase: true


---

#开发配置
spring:
  profiles: dev

  datasource:
    # url: jdbc:mysql://localhost:3306/man?useSSL=true&&characterEncoding=utf8
    url: jdbc:mysql://127.0.0.1:3306/stock_project?useSSL=true&&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
